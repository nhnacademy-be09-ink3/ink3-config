spring:
  cloud:
    openfeign:
      okhttp:
        enabled: true
server:
  servlet:
    context-path: /auth

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always