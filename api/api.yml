app:
  name: Spring cloud config
  version: 0.0.1

server:
  port: 10260

management:
  endpoints:
    web:
      exposure:
        include: health, refresh

api:
  message: default
  profile:
    active:
      message: config test